CONFIG_ZTEST=y

# CONFIG_FLASH=y
# CONFIG_FLASH_MAP=y
# CONFIG_SETTINGS=y
# CONFIG_SETTINGS_RUNTIME=y

# ZMS
#没有添加cache时，测试样例总耗时 [1.6s, 7s], when partition is 64K
#添加cache后，测试样例总耗时 [0.4s, 2.6s], when partition is 64K
# CONFIG_ZMS=y
# CONFIG_ZMS_LOOKUP_CACHE=y
# CONFIG_ZMS_LOOKUP_CACHE_SIZE=512

# NVS (slower)
#没有添加cache时，测试样例总耗时 [ 观察日志，耗时长，1分钟+ ], 会出现failed的情况
#添加cache后，测试样例总耗时 [1.7s, 7.2s], when partition is 64K
#添加cache后，测试样例总耗时 [0.6s, 3.0s], when partition is 16K
# CONFIG_NVS=y
# CONFIG_NVS_LOOKUP_CACHE=y
# CONFIG_NVS_LOOKUP_CACHE_SIZE=512
# CONFIG_SETTINGS_NVS_NAME_CACHE=y
# CONFIG_SETTINGS_NVS_NAME_CACHE_SIZE=512

# not work when open BT
# (cannot print out the latter part of testsuit logs)
# CONFIG_REALTEK_AMEBA_ZEPHYR_BT=y
# CONFIG_BT=y
# CONFIG_BT_OBSERVER=y
# CONFIG_BT_PERIPHERAL=y

CONFIG_LOG=y
CONFIG_LOG_MODE_IMMEDIATE=y